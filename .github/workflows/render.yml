name: Keep Render Alive

on:
  schedule:
    # Runs every 10 minutes
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: curl -I https://ims-y2il.onrender.com/api/health
